import styled from 'styled-components'

export const Mark = styled.mark`
  background-color: var(--blush);
  border-radius: 4px;
  color: var(--wine);
  font-size: 95%;
  font-weight: 500;
  padding: 2px 6px;
`
